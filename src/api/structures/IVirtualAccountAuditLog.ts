import { tags } from "typia";

/**
 * 감사로그 단건 엔터티. 관리자, 고객, 시스템 행위의 감사/추적 목적.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountAuditLog = {
  id?: string & tags.Format<"uuid">;
  actor_id?: string & tags.Format<"uuid">;
  actor_type?: string;
  event_type?: string;
  target_type?: string;
  target_id?: string & tags.Format<"uuid">;
  description?: string;
  occurred_at?: string & tags.Format<"date-time">;
};
export namespace IVirtualAccountAuditLog {
  /** 감사로그 리스트(행위자, 이벤트타입, 타깃, 기간 등) 조건과 페이징 구조. 보안, 대시보드, 내역 검색 필터 DTO. */
  export type IRequest = {
    actor_id?: string & tags.Format<"uuid">;
    event_type?: string;
    target_type?: string;
    from_date?: string & tags.Format<"date-time">;
    to_date?: string & tags.Format<"date-time">;
    page?: number & tags.Type<"int32">;
    limit?: number & tags.Type<"int32">;
  };

  /** 감사로그 신규 생성DTO. 행위자, 이벤트, 목표 등 필수. 감사이벤트 등록에만 사용. */
  export type ICreate = {
    actor_id: string & tags.Format<"uuid">;
    actor_type: string;
    event_type: string;
    target_type: string;
    target_id?: string & tags.Format<"uuid">;
    description?: string;
    occurred_at: string & tags.Format<"date-time">;
  };

  /** 감사로그 설명/결과 등 일부 속성 patch용 DTO. 대부분 사후행위 보정이나 에러코드 추가시만 사용. */
  export type IUpdate = {
    description?: string;
  };
}

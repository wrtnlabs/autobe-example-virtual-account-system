import { tags } from "typia";

/**
 * 알림 이벤트로그 단건. status, 상세(에러/trace), 기록시각 등 포함.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountNotificationEventLog = {
  id?: string & tags.Format<"uuid">;
  notification_id?: string & tags.Format<"uuid">;
  status?: string;
  detail?: string;
  logged_at?: string & tags.Format<"date-time">;
};
export namespace IVirtualAccountNotificationEventLog {
  /** 알림 이벤트로그(발송시도, 실패 등) 검색/페이징DTO. */
  export type IRequest = {
    notification_id?: string & tags.Format<"uuid">;
    status?: string;
    logged_from?: string & tags.Format<"date-time">;
    logged_to?: string & tags.Format<"date-time">;
    page?: number & tags.Type<"int32">;
    limit?: number & tags.Type<"int32">;
  };

  /** 알림 이벤트 로그 신규 등록 DTO. status, desc, 발생시각 포함. */
  export type ICreate = {
    notification_id: string & tags.Format<"uuid">;
    status: string;
    detail?: string;
    logged_at: string & tags.Format<"date-time">;
  };

  /** 알림 이벤트로그 일부 속성 patch용 DTO. */
  export type IUpdate = {
    status?: string;
    detail?: string;
  };
}

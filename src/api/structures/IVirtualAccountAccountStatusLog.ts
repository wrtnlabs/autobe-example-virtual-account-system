import { tags } from "typia";

/**
 * 가상계좌 상태 변경 이력을 나타냅니다.
 *
 * 이는 가상계좌의 상태 전환(예: 활성→만료 등)을 추적하는 엔터티로, 시스템 감사 및 리포트 기능에서 활용됩니다. 각 이력은 계좌별,
 * 담당자별로 명확히 구분되며, 규정 준수를 위해 별도 관리됩니다.
 *
 * Prisma virtual_account_account_status_logs 테이블의 스키마 및 설명에 직접 매핑됩니다.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountAccountStatusLog = {
  /**
   * 계좌 상태 변경 로그의 고유 식별자입니다.
   *
   * Prisma virtual_account_account_status_logs.id 컬럼의 설명에 따름.
   */
  id: string & tags.Format<"uuid">;

  /**
   * 상태 변경 대상 가상계좌의 고유 ID입니다.
   *
   * Virtual_account_virtual_accounts.id를 참조하여 계좌별 상태 이력 관리 목적입니다.
   */
  virtual_account_id: string & tags.Format<"uuid">;

  /**
   * 이 상태 변경을 수행한 관리자(admin)의 고유 ID입니다.
   *
   * Virtual_account_admins.id 관계이며, 추적 및 감사 목적입니다.
   */
  admin_id: string & tags.Format<"uuid">;

  /**
   * 변경 전 상태입니다. 예: 'ACTIVE'
   *
   * 가상계좌의 기존 상태를 기록합니다. Prisma 정의 참조.
   */
  from_status: string;

  /**
   * 변경 후 상태입니다. 예: 'EXPIRED'
   *
   * 가상계좌의 변경된 상태를 기록합니다. Prisma 정의 참조.
   */
  to_status: string;

  /**
   * 상태 전환 사유(관리자/시스템 입력). 예: '만료 처리', '수동 종료'.
   *
   * 상태 변경의 명확한 추적 및 로그 목적.
   */
  reason: string;

  /**
   * 상태가 변경된 시각. ISO8601 표준 형식.
   *
   * Virtual_account_account_status_logs.changed_at 컬럼 참조. 시스템 시간 기준으로 기록됩니다.
   */
  changed_at: string & tags.Format<"date-time">;
};
export namespace IVirtualAccountAccountStatusLog {
  /** 가상계좌 상태변경 이력(로그) 조회 필터/페이징용 DTO. */
  export type IRequest = {
    /** 가상계좌 ID(필터) */
    virtual_account_id?: string & tags.Format<"uuid">;

    /** 관리자 ID(필터) */
    admin_id?: string & tags.Format<"uuid">;
    from_status?: string;
    to_status?: string;
    from_date?: string & tags.Format<"date-time">;
    to_date?: string & tags.Format<"date-time">;
    page?: number & tags.Type<"int32">;
    limit?: number & tags.Type<"int32">;
  };

  /**
   * 가상계좌 상태 변경 이력(ICreate)은 새로운 상태 로그 라인 추가를 위한 입력 데이터입니다.
   *
   * 상태 변경을 API 또는 행정처리로 기록할 때 사용되며, 기존 상태/바뀐 상태, 사유, 시점, 관련 관리자 등 관계를 모두 지정해야
   * 합니다. 가상계좌 감사 및 변경관리를 위해 필수입니다.
   */
  export type ICreate = {
    /**
     * 상태 변경 대상 가상계좌의 고유 ID입니다.
     *
     * Virtual_account_virtual_accounts.id를 참조합니다.
     */
    virtual_account_id: string & tags.Format<"uuid">;

    /**
     * 이 상태 변경을 수행하는 관리자(admin)의 고유 ID입니다.
     *
     * Virtual_account_admins.id 관계입니다.
     */
    admin_id: string & tags.Format<"uuid">;

    /**
     * 변경 전 상태. 예: 'ACTIVE' 등.
     *
     * Prisma 정의의 가상계좌 상태값을 입력합니다.
     */
    from_status: string;

    /** 변경 후 상태. 예: 'EXPIRED' 등. */
    to_status: string;

    /** 상태 변경 사유. 관리자/시스템이 입력합니다. */
    reason: string;

    /** 상태 변경 시각. 기본적으로 시스템 생성시 입력, 필요시 수동 지정. */
    changed_at: string & tags.Format<"date-time">;
  };
}

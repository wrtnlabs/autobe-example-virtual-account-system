import { tags } from "typia";

/**
 * 가상계좌 정보 단일 오브젝트. 계좌번호, 만료일, 계좌상태, 소유자 등. admin/고객/연동/감사 화면에서 사용.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountVirtualAccount = {
  id: string & tags.Format<"uuid">;
  customer_id: string & tags.Format<"uuid">;
  bank_id: string & tags.Format<"uuid">;
  pg_provider_id: string & tags.Format<"uuid">;
  account_number: string;
  expire_at: string & tags.Format<"date-time">;
  status: string;
  holder_name: string;
  created_at: string & tags.Format<"date-time">;
  updated_at: string & tags.Format<"date-time">;
  closed_at?: (string & tags.Format<"date-time">) | null;
};
export namespace IVirtualAccountVirtualAccount {
  /** 가상계좌 목록 조회, 필터링/페이징 요청 DTO. */
  export type IRequest = {
    /** 고객 ID(필터) */
    customer_id?: string & tags.Format<"uuid">;

    /** 계좌 상태(예: ACTIVE, EXPIRED 등) */
    status?: string;

    /** 은행(필터) */
    bank_id?: string & tags.Format<"uuid">;

    /** PG사(필터) */
    pg_provider_id?: string & tags.Format<"uuid">;

    /** 계좌번호 일치검색 */
    account_number?: string;

    /** 만료일 검색 시작일 */
    expire_from?: string & tags.Format<"date-time">;

    /** 만료일 검색 종료일 */
    expire_to?: string & tags.Format<"date-time">;
    page?: number & tags.Type<"int32">;
    limit?: number & tags.Type<"int32">;
  };

  /** 가상계좌 신규 발급/생성 요청 DTO. 사용자/은행/PG정보 등 필수 입력. */
  export type ICreate = {
    /** 발급 대상 고객 PK */
    customer_id: string & tags.Format<"uuid">;
    bank_id: string & tags.Format<"uuid">;
    pg_provider_id: string & tags.Format<"uuid">;
    account_number: string;
    expire_at: string & tags.Format<"date-time">;
    status: string;
    holder_name: string;
  };

  /** 가상계좌 정보(상태, 만료일 등) 수정 DTO. 소유고객, 계좌번호 등 일부 필드 수정 불가. */
  export type IUpdate = {
    status?: string;
    expire_at?: string & tags.Format<"date-time">;
    holder_name?: string;
  };
}

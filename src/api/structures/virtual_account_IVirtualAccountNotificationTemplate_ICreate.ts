import { tags } from "typia";

/**
 * 알림 템플릿(virtual_account_notification_templates) 신규 등록 요청 스키마.
 *
 * 템플릿코드, 채널, 제목, 포맷, 언어, 버전 등 모든 핵심 속성을 신규 생성시 지정하며,
 *
 * 테이블 구조의 제약조건(중복불가, 유효범위)을 준수합니다.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type virtual_account_IVirtualAccountNotificationTemplate_ICreate = {
  /**
   * 생성할 템플릿 고유 코드. 논리 구분 식별자.
   *
   * PK 아님, 채널+언어+버전 중복 불가.
   */
  template_code: string;

  /**
   * 알림 채널구분 (카카오톡, 이메일, SMS 등).
   *
   * Template/channel 매핑.
   */
  channel_type: string;

  /**
   * 템플릿 제목이나 메일제목 등.
   *
   * 긴 텍스트 허용.
   */
  title: string;

  /**
   * 본문 형식(text/html/markdown)
   *
   * 채널용 구분자.
   */
  body_format: string;

  /**
   * 템플릿 본문 - 치환 변수 포함 가능.
   *
   * 템플릿 엔진에서 동적 렌더링 지원.
   */
  body_template: string;

  /**
   * 언어 구분자. 예: 'ko', 'en', 'zh'.
   *
   * 다국어 템플릿 관리 필수 컬럼.
   */
  language_code: string;

  /**
   * 버전 번호(정수형).
   *
   * 변경이력 및 diff 관리 용도.
   */
  version: number & tags.Type<"int32">;

  /**
   * 활성화 여부. 신규 발송 적용 가능 여부 지정.
   *
   * 업무 관리/운영 정책 지원.
   */
  is_active: boolean;
};

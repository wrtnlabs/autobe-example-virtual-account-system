import { tags } from "typia";

/**
 * 알림 템플릿 엔터티(virtual_account_notification_templates) 업데이트 요청에 사용하는 body 스키마.
 *
 * 변경 대상 컬럼만 부분 입력 가능하며, 정책상 불변속성은 제외되었습니다.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type virtual_account_IVirtualAccountNotificationTemplate_IUpdate = {
  /**
   * 수정할 템플릿의 논리 코드.
   *
   * 필요시만 입력.
   */
  template_code?: string;

  /**
   * 템플릿 채널 구분 수정.
   *
   * 필요시만 입력.
   */
  channel_type?: string;

  /**
   * 템플릿 제목, 메일제목 등.
   *
   * 필요시만 입력.
   */
  title?: string;

  /**
   * 본문 형식(text/html/markdown) 변경.
   *
   * 필요시만 입력.
   */
  body_format?: string;

  /**
   * 템플릿 메시지 본문(치환변수포함).
   *
   * 필요시만 입력.
   */
  body_template?: string;

  /**
   * 언어코드 변경.
   *
   * 필요시만 입력.
   */
  language_code?: string;

  /**
   * 버전 변경시 지정.
   *
   * 템플릿 변경 이력 관리 시 필요.
   */
  version?: number & tags.Type<"int32">;

  /** 활성/비활성화 토글 변경 시 지정. */
  is_active?: boolean;
};

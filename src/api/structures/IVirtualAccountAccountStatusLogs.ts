import { tags } from "typia";

/**
 * 상태 변경 이력 엔터티(virtual_account_account_status_logs)의 전체 필드를 포함합니다.
 *
 * 계좌/관리자/변경시각별 상태 변동을 완전히 추적 가능하게 합니다. 상세 감사 및 상태 흐름 구조도 표현에 사용됩니다.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountAccountStatusLogs = {
  /** 상태 변경 이력 고유 식별자. */
  id: string & tags.Format<"uuid">;

  /** 연결된 가상계좌 ID. */
  virtual_account_id: string & tags.Format<"uuid">;

  /** 상태 변경 담당 관리자 ID. */
  admin_id: string & tags.Format<"uuid">;

  /** 변경 전 상태. */
  from_status: string;

  /** 변경 후 상태. */
  to_status: string;

  /** 상태 변경 사유/주석. */
  reason: string;

  /** 상태 변경 시각. */
  changed_at: string & tags.Format<"date-time">;
};
export namespace IVirtualAccountAccountStatusLogs {
  /**
   * 상태 변경 이력 레코드의 부분/전체 수정용 인터페이스입니다.
   *
   * 상태,사유,관리자 지정 값 중 일부 또는 전체를 변경할 수 있습니다. 트랜잭션 및 감사지원 용도로 사용됩니다.
   */
  export type IUpdate = {
    /**
     * 수정할 경우 사용되는 변경 전 상태값(정상적으로 사용 빈도 낮음).
     *
     * 일반적으로 초기값이 맞을 경우 생략 가능.
     */
    from_status?: string;

    /**
     * 변경 후 상태값 신규 지정.
     *
     * 일부 이력정정/재입력시 활용.
     */
    to_status?: string;

    /** 상태 변경 이력의 수정 사유 혹은 설명(감사 추적 목적). */
    reason?: string;

    /** 수정 처리자를 별도로 지정 가능. 미지정시 기존 기록 유지. */
    admin_id?: string & tags.Format<"uuid">;

    /** 수정되는 상태 변경 시점. 특별 정정이 필요한 경우 명시. */
    changed_at?: string & tags.Format<"date-time">;
  };

  /**
   * 상태 변경 이력 삭제 요청에 대한 결과 데이터 구조입니다.
   *
   * 성공 여부, 삭제된 PK, 필요시 오류 메시지 등을 제공합니다.
   */
  export type IDeleteResult = {
    /** 삭제 처리 성공 여부 플래그. true인 경우 정상적으로 삭제된 것임. */
    success: boolean;

    /** 정상 삭제된 상태 이력 레코드의 고유 ID. */
    deletedId: string & tags.Format<"uuid">;

    /** 삭제 결과에 대한 결과 메시지 또는 오류 원인(없을 수도 있음). */
    message?: string;
  };
}

import { tags } from "typia";

/**
 * ERP 벤더/타입 정보 마스터 테이블 행의 구조입니다.
 *
 * ERP 연동, 관리화면, 외부연동설정 등에서 ERP별 코드와 표준명칭, 등록일자를 제공합니다.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountErpTypes = {
  /**
   * ERP 타입 엔터티의 고유 식별자입니다.
   *
   * Prisma의 id 컬럼과 연결되며, ERP 벤더 구분/조회 등에서 사용됩니다.
   */
  id: string & tags.Format<"uuid">;

  /**
   * ERP 타입 코드.
   *
   * Prisma 스키마 code 컬럼에 따라 중복 불가/PK성질을 지닙니다.
   */
  code: string;

  /**
   * ERP 벤더/타입의 한글 표기.
   *
   * ERP UI, 관리자 화면, 연동 업무 등에 활용됩니다.
   */
  display_name: string;

  /**
   * 등록일자(ISO8601).
   *
   * 테이블 생성 시각기록 및 색인에 활용.
   */
  created_at: string & tags.Format<"date-time">;
};
export namespace IVirtualAccountErpTypes {
  /**
   * ERP 타입 목록(코드, 명칭, 등록일 등) 검색/필터용 요청 구조입니다.
   *
   * 요구 조건에 따른 필드만 선택적으로 전달합니다.
   */
  export type IRequest = {
    /**
     * 검색/필터/정렬을 위한 ERP 타입 코드값.
     *
     * ERP 벤더 구분 등으로 사용되며, Prisma 엔티티 필드와 연동됩니다.
     */
    code?: string;

    /**
     * ERP 표준명/검색명.
     *
     * 필터, 검색, UI 캡션 등에 활용.
     */
    display_name?: string;

    /** 생성일 검색 시작(ISO8601). */
    created_from?: string & tags.Format<"date-time">;

    /** 생성일 검색 종료(ISO8601). */
    created_to?: string & tags.Format<"date-time">;

    /** 페이지 번호(기본 1). */
    page?: number &
      tags.Type<"int32"> &
      tags.Default<1> &
      tags.JsonSchemaPlugin<{
        format: "int32";
      }>;

    /** 페이지 당 레코드 수(기본 20). */
    size?: number &
      tags.Type<"int32"> &
      tags.Default<20> &
      tags.JsonSchemaPlugin<{
        format: "int32";
      }>;
  };

  /**
   * ERP 타입(벤더) 마스터 신규 생성 요청 구조입니다.
   *
   * Code, display_name 필수 입력이며, 중복 입력 제약이 있습니다.
   */
  export type ICreate = {
    /**
     * 신규 ERP 타입(벤더) 코드.
     *
     * Prisma 스키마 고유 제약조건에 맞춰 입력 필요.
     */
    code: string;

    /**
     * ERP 벤더명/표기명.
     *
     * 관리자 UI, 연동설정 등의 표기필드.
     */
    display_name: string;
  };
}

import { tags } from "typia";

/**
 * 관리자 경보/알림 엔티티 대표 타입입니다.
 *
 * Virtual_account_admin_alerts 테이블의 1행과 1:1 매핑되며, 대량 미매칭, 외부 연동 장애, 보안 경고 등 다양한
 * 시스템 이벤트를 포괄합니다.
 *
 * 관리자 대시보드에서의 현황판/실시간 알림, 경보 리스트, 상세 내역, Excel 내보내기 등에 사용됩니다.
 *
 * @autobe Generated by AutoBE - https://github.com/wrtnlabs/autobe
 */
export type IVirtualAccountAdminAlerts = {
  /**
   * 경보/알림의 고유 식별자.
   *
   * Virtual_account_admin_alerts.id와 1:1로 매핑됩니다.
   */
  id: string & tags.Format<"uuid">;

  /**
   * 알림을 수신하는 대상 관리자 UUID.
   *
   * 특정 관리자 지정용이며, 시스템 전체 알림은 null 가능합니다.
   */
  virtual_account_admin_id?: (string & tags.Format<"uuid">) | null;

  /**
   * 경보/이상 이벤트 유형 코드. (예: MASS_MISMATCH, EXTERNAL_ERROR, SECURITY_WARNING 등)
   *
   * Virtual_account_admin_alerts.alert_type의 비즈니스 해석값입니다.
   */
  alert_type: string;

  /**
   * 경보/알림의 상세 데이터(JSON 문자열).
   *
   * 이벤트 관련 상세 설명, 대상 엔티티, 에러 context 등 임의의 구조.
   */
  context_json: string;

  /**
   * 해당 알림이 시스템에 생성/노출된 시각(UTC, ISO8601).
   *
   * Virtual_account_admin_alerts.notified_at에 직접 매핑.
   */
  notified_at: string & tags.Format<"date-time">;

  /**
   * UI상 '확인/해제' 가능 여부 표시.
   *
   * 관리자가 수동으로 경보를 해지/확인할 수 있으면 true.
   */
  clearable: boolean;

  /**
   * 관리자에 의해 알림이 확인(처리 종료 등)된 시각.
   *
   * Null이면 아직 미확인 상태.
   */
  cleared_at?: (string & tags.Format<"date-time">) | null;

  /**
   * 해당 알림에 대한 부가 설명, 관리자에게 표시할 요약/해설/비고. (Optional)
   *
   * 예: 특정 이슈에 부연설명 제공 목적.
   */
  description?: string | null;
};
export namespace IVirtualAccountAdminAlerts {
  /**
   * 관리자 알림/경보 조회용 요청 필터 타입.
   *
   * 관리자 알림 리스트/검색 API의 파라미터 타입으로, 관리자별, 알림유형, 발송일, 읽음/해제 상태 등 다양한 조건 필터를
   * 제공합니다.
   */
  export type IRequest = {
    /**
     * 대상 관리자 ID(필터)
     *
     * 지정 관리자 알림만 필터링. Prisma: `virtual_account_admin_id`.
     */
    virtual_account_admin_id?: string & tags.Format<"uuid">;

    /**
     * 알림/경보 유형 (EX: MASS_MISMATCH, EXTERNAL_ERROR 등)
     *
     * Prisma: `alert_type`. 유형별 분류 검색.
     */
    alert_type?: string;

    /**
     * 알림 생성/발송 검색 시작일(ISO8601)
     *
     * Prisma: `notified_at` 컬럼 검색에 활용.
     */
    notified_from?: string & tags.Format<"date-time">;

    /**
     * 알림 생성/발송 검색 종료일.
     *
     * Prisma: `notified_at` 컬럼 검색에 활용.
     */
    notified_to?: string & tags.Format<"date-time">;

    /** 알림 읽음처리(삭제가능) 여부 필터. Prisma: `clearable`. */
    clearable?: boolean;

    /**
     * 알림 실제 read/clear 처리됨 여부(해당시 true). Prisma: `cleared_at` 널이 아닌 경우
     * true.
     */
    cleared?: boolean;
  };
}
